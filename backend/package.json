{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/bundle.mjs",
    "build:types": "tsc --noEmit",
    "start": "npm run build && node scripts/start-local.mjs",
    "dev": "npm run start",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.819.0",
    "@aws-sdk/lib-dynamodb": "^3.819.0",
    "vite": "8.2.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
